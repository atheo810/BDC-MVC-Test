<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("../../setups/head") %>
</head>

<body>
  <%- include("../../components/navbar") %>

  <section id="home-content" class="container">
    <div class="my-5">
      <div class="align-items-center d-flex justify-content-between mt-5 mb-4">
        <h3>Detail</h3>
      </div>
      <div class="d-flex">
        <div>
          <img class="border mb-3" width="350"
            src="/uploads/<%= book.img %>"
            alt="book" />
        </div>
        <div class="ms-5">
          <h5><%= book.merk %></h5>
          <h2><%= book.name %></h2>
          <p class="h6 my-4">Availability: <%= book.available %></p>
          <p class="h5">Rp <%= book.price %></p>
          <p class="my-4">Hurry up! Only <%= book.qty %> product left in stock!</p>
          <p><%= book.desc %></p>
        </div>
      </div>
    </div>
  </section>

  <%- include("../../setups/script") %>

</html>